spring.application.name=Vendas

# --- Configura��o do Banco de Dados H2 ---
spring.datasource.url=jdbc:h2:mem:vendasdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=ingrid
spring.datasource.password=0001

# --- Inicializa��o do banco e scripts ---
spring.sql.init.mode=always
spring.jpa.hibernate.ddl-auto=none

# --- Mostrar console web do H2 ---
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# --- Configura��o do JPA / Hibernate ---
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.show-sql=true
